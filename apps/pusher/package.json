{
  "name": "pusher",
  "module": "./src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "start":"bun ./src/index.js",
    "dev":"bun run build && bun run start"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@repo/store": "*",
    "@repo/typescript-config":"*",
    "@repo/redis-stream": "*"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
